{
  "name": "@cognitionlabs/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "studio": "prisma studio",
    "seed": "ts-node --esm prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.0.1"
  },
  "devDependencies": {
    "prisma": "^6.0.1",
    "typescript": "^5.6.2",
    "ts-node": "^10.9.2"
  }
}
